interface:
  display_name: "Principle Fix Root Causes"
  short_description: "Apply fix root causes with discipline"
  default_prompt: "Use $principle-fix-root-causes to apply this engineering principle to the current decision."
