interface:
  display_name: "Recall"
  short_description: "Reconstruct recent project context from scoped records"
  default_prompt: "Use $recall to handle this task with the pstack workflow and return a verified result."
